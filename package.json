{
	"license": "MIT",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"build:test": "tsc -p tsconfig.test.json",
		"test": "npm run build:test && node --test .test-dist/test/api/*.test.js .test-dist/test/lib/*.test.js",
		"dev": "vercel dev",
		"deploy": "vercel --prod"
	},
	"devDependencies": {
		"@types/node": "^25.9.3",
		"@vercel/node": "^5.8.17",
		"typescript": "^6.0.2"
	},
	"dependencies": {
		"@vercel/blob": "^2.4.1",
		"bcryptjs": "^3.0.3",
		"jose": "^6.2.2",
		"redis": "^6.0.0"
	}
}
